name: Build & Release APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build web assets
        run: npm run build

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Set Android SDK env vars
        run: |
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "PATH=$ANDROID_SDK_ROOT/platform-tools:$PATH" >> $GITHUB_ENV

      # Keystore'u cache'le — aynı imza her build'de kullanılsın
      - name: Cache debug keystore
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: wattly-debug-keystore-v1

      - name: Ensure debug keystore exists
        run: |
          mkdir -p ~/.android
          if [ ! -f ~/.android/debug.keystore ]; then
            keytool -genkey -v \
              -keystore ~/.android/debug.keystore \
              -alias androiddebugkey \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -storepass android -keypass android \
              -dname "CN=Android Debug,O=Android,C=US"
          fi

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      # cap copy: sadece web dosyalarını android projesine kopyalar (SDK gerektirmez)
      - name: Copy web assets to Android
        run: npx cap copy android

      - name: Build debug APK
        run: cd android && ./gradlew assembleDebug --no-daemon

      - name: Get version from package.json
        id: pkg
        run: echo "VERSION=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Rename APK
        run: |
          cp android/app/build/outputs/apk/debug/app-debug.apk \
             "wattly-v${{ steps.pkg.outputs.VERSION }}.apk"

      # Aynı versiyon etiketi varsa sil
      - name: Delete existing release if same tag exists
        run: gh release delete "v${{ steps.pkg.outputs.VERSION }}" --yes 2>/dev/null || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Delete existing tag if exists
        run: git push origin --delete "v${{ steps.pkg.outputs.VERSION }}" 2>/dev/null || true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.pkg.outputs.VERSION }}
          name: "Wattly v${{ steps.pkg.outputs.VERSION }}"
          body: |
            🤖 Otomatik build
            📦 Commit: ${{ github.sha }}
            🕐 Tarih: ${{ github.event.head_commit.timestamp }}

            **Kurulum:**
            1. `wattly-v${{ steps.pkg.outputs.VERSION }}.apk` dosyasını indirin
            2. Telefonda "Bilinmeyen kaynaklar" izni açık olmalı
            3. APK'yı kurun — mevcut uygulamanın üzerine yükler
          files: wattly-v${{ steps.pkg.outputs.VERSION }}.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
